[lss-headscale-dashboard]
enabled  = true
filter   = lss-headscale-dashboard
backend  = systemd
journalmatch = _SYSTEMD_UNIT=lss-headscale-dashboard.service
maxretry = 5
findtime = 10m
bantime  = 1h
# Ban at the edge — adjust action/port for your HAProxy/NGINX firewall stack.
port     = http,https
